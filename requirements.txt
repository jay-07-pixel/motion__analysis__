# Python packages for this project.
# mediapipe 0.10.14 = local Pose (33 joints). Do not use mediapipe 1.x here:
# it needs a different API and pulled NumPy 2, which broke OpenCV.

pyrealsense2==2.58.4.10922
opencv-python>=4.8.0
PyYAML>=6.0
numpy>=1.23.0,<2.0
mediapipe==0.10.14
